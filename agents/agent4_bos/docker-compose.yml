version: '3.8'

services:
  agent4_bos:
    build: .
    container_name: agent4_bos
    ports:
      - "8004:8000"
    networks:
      - ai_network
    environment:
      - COLLECTION=agent4_bos_cases
      - QDRANT_HOST=qdrant
      - QDRANT_PORT=6333
networks:
  ai_network:
    external: true